# Spotify OAuth2 Configuration
SPOTIFY_CLIENT_ID="your-spotify-client-id"
SPOTIFY_CLIENT_SECRET="your-spotify-client-secret"
# Use your Render backend URL below (update after deploying)
SPOTIFY_REDIRECT_URI="https://sonicmirror-backend.onrender.com/auth/callback"  # Must match Spotify Developer Dashboard

# Frontend URL (for redirects after OAuth)
# Use your Render frontend URL below (update after deploying)
FRONTEND_URI="https://sonicmirror-frontend.onrender.com/dashboard"

# Server Configuration
PORT=3001
NODE_ENV=production

# OpenAI Configuration (for LLM features)
OPENAI_API_KEY="your-openai-api-key"

# Database Configuration (optional - for MongoDB)
MONGODB_URI="mongodb://localhost:27017/sonicmirror"

# JWT Secret (for token signing)
JWT_SECRET="your-jwt-secret"

# Gemini API Key (for Google Generative AI)
GEMINI_API_KEY=your-gemini-api-key-here

# NOTE:
# - Set SPOTIFY_REDIRECT_URI and FRONTEND_URI to your actual Render URLs after deploying.
# - These URLs must also be set in your Spotify Developer Dashboard for OAuth to work.